server:
  bind_addr: "[::1]:50051"

# path for ml model folder

models:
  - name: "model4"
    device: "cpu"
    queue_capacity: 16

  - name: "model5"
    device: "cpu"
    queue_capacity: 16

  - name: "model3"
    device: "cpu"
    queue_capacity: 16